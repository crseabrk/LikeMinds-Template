# Migration

Document source and destination versions, compatibility checks, exact files affected, operational-data impact, backup, rollback, validation, and required human approvals.

No migration may silently rewrite append-only history or expand permissions.
